function tinyFriend(names){
   var tiny = names[0];
   for(var i = 0; i < names.length; i++){
      list = names[i];
      if(list.length < tiny.length){
        tiny = list;
         }

           }
    return tiny;
}
var result = tinyFriend(["mahi", "haseam", "jamal"]);
console.log(result);

